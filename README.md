# rust-chain

A small proof-of-work blockchain in Rust with deterministic block construction, validation, and a concurrent miner.

## Running the Demo

```bash
cargo run -- [chain_length] [difficulty_bits] [workers]
```
